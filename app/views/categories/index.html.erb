<p style="color: green"><%= notice %></p>

<h1>Categories</h1>

<div id="categories">
  <% @categories.each do |category| %>
    <%= render category %>
    <div>
      <%= link_to category.name, category_items_path(category.id) %>
    </div>
    <img src="<%= category.icon %>", alt="shopping_image">
  <% end %>
</div>

<%= link_to "Add category", new_category_path %>
